# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI local state (project ref, pooler cache) — machine-local, not source
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite writes these while loading vite.config.ts and normally deletes them
# again — leftovers only appear when a dev-server process died mid-start.
vite.config.ts.timestamp-*.mjs

# Local scratch, not part of the site:
#   partner/  — the original partner-logo downloads (22.08.2026). The versions
#               the site actually ships are src/assets/partner-*.webp.
#   prompt.md — Almedins Notizzettel.
partner/
prompt.md
